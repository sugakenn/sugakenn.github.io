---
layout: post
title: "MarkDownの基本"
date: 2026-07-23 17:00:00 +0900
description: github pagesを使うようになったので、MarkDownの記法を覚えることにしました
img: 2026/2026-07-23-1.png # Add image post (optional)
fig-caption: # Add figcaption (optional)
tags: [pc_tips]
categories: 
  - pc_tips
---

筆者が、ここに書くことは自分の覚書をどこからでも見られるようにしておく為です。
以前までBloggerを使っていたのですが、いろいろ触っていたらGoogleにキャッシュされなくなってしまったので、githubに移ってきました。

こちらだと、Jekyllを使ってMarkDownを使って書くことができるので、これを機にMarkDownを覚えることにします。

---

# 原則

マークダウンでは基本的に、文字コードはUTF-8、改行はLFで記述します。

マークダウン専用の記述式だけでなく、多くのHTMLタグが使えます。ただし、HTMLタグの内側ではマークダウンの書式は使えません。

---

# 見出し

`#` の数で見出しのレベルを指定します。

## 書き方

```md
# 見出し1
## 見出し2
### 見出し3
#### 見出し4
```

---

# 段落

空行を入れると新しい段落になります。

## 書き方

```md
これは1つ目の段落です。

これは2つ目の段落です。
```

---

# 改行

Markdownでは改行だけでは改行されません。

## 行末にスペース2つ

```md
1行目␠␠
2行目
```

※「␠」はスペースを表します。

または

```md
1行目<br>
2行目
```

---

# 強調

## 太字

```md
**重要**
```

## 斜体

```md
*重要*
```

## 太字＋斜体

```md
***重要***
```

## 取り消し線

```md
~~削除~~
```

---

# リンクと画像

リンクと画像は異なるものですが、記法が似ているのでまとめて説明します。

```md
[表示するテキスト](URL)

![画像の代替テキスト](URLまたはファイルパス)
```

---

# 水平線

```md
---
```

---

# 箇条書き

## 書き方

```md
- りんご
- みかん
- バナナ
```

## 入れ子

```md
- 親
  - 子
    - 孫
```

---

# 番号付きリスト

```md
1. 赤
2. 青
3. 黄
```

---

# チェックリスト（GitHub対応）

```md
- [ ] 未完了
- [x] 完了
```

---

# 引用

```md
> 引用です。
>> 二重引用です。
```

---

# インラインコード

文章中にコードを書きたい場合はバッククォートで囲みます。

```md
`printf()`
```

---


# コードブロック

複数行のコードはバッククォート3つで囲みます。

````text
```c
printf("Hello");
```


```python
```java
```c
```php
```sql
```bash
```json
```text

````

コードブロックの開始時に言語を指定すると、それに合わせたハイライトが行われます。

また、コードブロックのバッククォートは3つ以上も設定でき、数が多い場合はその数以上のバッククオートが表れれない限りコードブロックが終了しません。
(コードブロック内に```を書きたい場合などに使い買います)

---

# テーブル

テーブルは \| と - を使って表します。テーブルのセル内でもマークダウンを使うことができます。


```md
| タイトル    |          |   |
| ----------- | -------- | - |
| 内容        |          |  |

```
---はヘッダとデータの区分けにつかいますが、ここで

:--- とすれば左寄せ

---: とすれば右寄せ

:----:とすれば中央寄せ

となります。

---

# エスケープ

\# や　* などをエスケープしたい場合は \ をつけます。気を付けないといけないのは不要なエスケープをすると\はそのまま文字列として扱われてしまうところです。

またhtmlタグを例示したいような場合は\`で囲んでコードブロックにすることでエスケープが可能です。  
同様に` ``` `で囲まれたブロック内なら、エスケープは不要になります。

コメントはマークダウンの記法としては存在しませんが、HTMLのタグ`<!-- -->` が使えます。


